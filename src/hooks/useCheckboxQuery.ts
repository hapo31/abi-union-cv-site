export type CheckboxValue = {
  arknights?: boolean;
  bluearchive?: boolean;
  imasCinderella?: boolean;
};

type Props = {
  defaultValue?: CheckboxValue;
};

export default function useCheckboxQuery() {}
